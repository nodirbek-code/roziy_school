# Django
DJANGO_SECRET_KEY=change-me-to-a-random-secret-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=*

# Database (leave DB_ENGINE unset to use local SQLite)
DB_ENGINE=postgres
DB_NAME=roziy_maktabi
DB_USER=postgres
DB_PASSWORD=6690
DB_HOST=localhost
DB_PORT=5432

# CORS - frontend origin(s), comma separated
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
